// Font variables are defined in src/styles/theme.css with system fallbacks.
// Keeping this export preserves the layout API without requiring Google Fonts at build time.
export const fontVariables = '';
